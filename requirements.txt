openenv-core
pydantic>=2.0.0
openai
requests
uvicorn
fastapi
numpy>=1.24
matplotlib>=3.7
pillow>=10.0
gradio>=5,<6
plotly
# Heavy ML stack is installed in a separate Dockerfile RUN with the
# pytorch CPU index so we don't pull ~2GB of CUDA wheels for a CPU-only Space.
# torch / transformers / peft / accelerate / huggingface_hub
